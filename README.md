The MDP is presented in loop_1_MAIN_RL and the code can be trained by PPO agent using the code train_charging_pod_rl.

The results of the training can be evaluated using evaluate_charging_pod_rl.

The baseline model is presented in loop_1_baseline and another modified baseline method in loop_1_MAIN_mod. These code can be executed from the terminal.

THe relavant SUMO files are also attached.
